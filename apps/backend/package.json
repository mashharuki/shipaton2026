{
  "name": "backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply seatsignal-db --local",
    "generate:datasets": "tsx scripts/generate-datasets.ts",
    "ingest:toei": "tsx scripts/ingest-toei-gtfs.ts",
    "push:datasets": "tsx scripts/push-datasets-to-kv.ts",
    "setup:local": "pnpm run db:migrate:local && pnpm run push:datasets",
    "openapi": "tsx scripts/generate-openapi.ts"
  },
  "dependencies": {
    "@hono/zod-openapi": "1.5.1",
    "hono": "^4.12.33",
    "shared": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.20.1",
    "fflate": "0.8.3",
    "gtfs-realtime-bindings": "2.2.0",
    "tsx": "4.23.5",
    "vitest": "4.1.10",
    "wrangler": "^4.110.0",
    "yaml": "2.9.0"
  }
}
